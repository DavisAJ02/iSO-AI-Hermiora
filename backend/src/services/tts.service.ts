/**
 * Text-to-speech for voice step. Integrate ElevenLabs, OpenAI TTS, etc.
 */
export async function synthesizeSpeechStub(text: string): Promise<{ audioUrl?: string }> {
  void text;
  return { audioUrl: undefined };
}
