/**
 * Final video render (FFmpeg, Remotion, cloud encoder).
 */
export async function renderVideoStub(projectId: string): Promise<{ videoUrl?: string }> {
  void projectId;
  return { videoUrl: undefined };
}
